const pos =document.querySelectorAll('.start');
pos:addEventListener('change',double)
    // +e.target.value;
    // this.double(+e.target.value)
    
// } 
        
        
        const people = document.querySelector('.people');
        const com = document.querySelector('#com');
        people.addEventListener('change',(e)=>{
            // console.log(typeof +e.target.value);

            com.textContent = +e.target.value;
            // console.log(people.options.selectedIndex);
            double=(e)=>{
                this.e.target.value
                console.log(this.e.target.value);
                com.textContent =value + e.target.value
            }
            
        })
        


        
        